# version

## Overview

Package version string. Sets `window.__ORDERLY_VERSION__["@veltodefi/hooks"]` in browser for debugging.

## Exports

- **default**: `"2.9.1"` (version string)

## Usage example

```ts
import version from "@veltodefi/hooks";
console.log(version); // "2.9.1"
```
