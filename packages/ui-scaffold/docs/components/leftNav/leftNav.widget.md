# LeftNavWidget

> Location: `packages/ui-scaffold/src/components/leftNav/leftNav.widget.tsx`

## Overview

Widget that composes `useLeftNavScript` and `LeftNavUI`; accepts `LeftNavUIProps` (same as `LeftNavProps`).

## Usage example

```tsx
import { LeftNavWidget } from "@veltodefi/ui-scaffold";

<LeftNavWidget menus={[...]} telegramUrl="..." />
```
